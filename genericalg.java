//import files from main for 'population'=all info on classes

public class genericalg {
    int population;

    void println(compute_fitness());

    int compute_fitness(population){
        return 0;
    }
    for(int i=0;i>0;i++){
        //selection
        //crossover-----select parents for reproduction
        //mutation
        //compute fitness
        //loop until population had converged then stop
    }
}
